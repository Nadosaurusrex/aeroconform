model:
  name: aerogpt
  input_dim: 8
  hidden_dim: 256
  num_layers: 6
  num_heads: 8
  head_dim: 32
  feedforward_dim: 1024
  dropout: 0.1
  max_seq_len: 512
  output_type: gaussian
  output_dim: 16
