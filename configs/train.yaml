training:
  batch_size: 256
  learning_rate: 3.0e-4
  weight_decay: 0.01
  warmup_steps: 2000
  max_steps: 100000
  scheduler: cosine_with_warmup
  gradient_clip: 1.0
  mixed_precision: true
  checkpoint_every: 5000
  eval_every: 1000

masked:
  mask_ratio: 0.15
  max_steps: 20000
